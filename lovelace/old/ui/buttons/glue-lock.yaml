type: entity-button
entity: lock.glue_lock
name: Glue Lock
icon: fas:lock
color_type: icon
show_state: false
show_name: false
tap_action:
  action: call-service
  service: lock.lock
  service_data:
    entity_id: lock.glue_lock
style: |
  ha-card {
    height: auto !important;
    width: auto !important;
    margin: 5px;
    #border-radius: 10px;
    box-shadow: 0px 0px 10px 0px rgba(0,0,0,0.5) !important;
  }
  .card-header {
    font-size: 16px;
  }
  ha-icon {
    height: 35px !important;
    width: 30px !important;
    color: {% if is_state('lock.glue_lock', 'locked') %} hotpink !important {% endif %};
  }
  
