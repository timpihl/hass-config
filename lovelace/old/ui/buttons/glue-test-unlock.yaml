type: entity-button
entity: lock.glue_test
name: Glue Test Unlock
icon: mdi:lock-open
color_type: icon
show_state: false
show_name: false
tap_action:
  action: call-service
  service: lock.unlock
  service_data:
    entity_id: lock.glue_test
style:  |
  ha-card {
    margin: 5px;
    #border-radius: 10px;
    box-shadow: 0px 0px 10px 0px rgba(0,0,0,0.5) !important;
  }
  .card-header {
    font-size: 16px;
  }
  ha-icon {
    color: {% if is_state('lock.glue_test', 'unlocked') %} hotpink !important {% endif %};
  }
  