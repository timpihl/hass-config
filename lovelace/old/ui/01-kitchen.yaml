type: entity-button
entity: light.fonster_k
style: |
  @keyframes blink {
    50% {
      box-shadow: 0px 0px 25px -5px rgb(255,85,143);
    }
  }
  ha-card {
    border-radius: 15px;
    font-size: 0px !important;
    margin: 10px;
    animation: [[ if(light.fonster_k == "on", "blink 1.5s linear infinite", "") ]];
  }
  ha-icon {
    padding: 5px !important;
    height: 35px !important;
  }
