type: markdown
content: |
  {% if is_state("person.tim", "ToKer Home") and is_state("person.kanita", "ToKer Home") -%} 
  You are both home, you silly
  {% elif is_state("person.tim", "East") and is_state("person.kanita", "East") -%}
  You are both at East, you silly
  {%- else -%}
  Tim is at {{ states("person.tim") }}
  Kanita is at {{ states("person.kanita") }}
  {%- endif %}